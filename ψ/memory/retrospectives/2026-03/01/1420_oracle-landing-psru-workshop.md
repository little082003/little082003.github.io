# Session Retrospective — Oracle Landing Page & PSRU Workshop

**วันที่**: 2026-03-01
**เวลา**: ประมาณ 07:00 - 14:20 GMT+7
**ระยะเวลา**: ~7 ชั่วโมง
**โฟกัส**: Oracle Landing Page, Portfolio Integration, DNS Configuration, Deployment
**ประเภท**: Feature + Deployment + Research

---

## สรุปเซสชัน

เซสชันนี้เริ่มจากการแก้แบบจำลอง 3D (Laevatain.glb) ใน workshop-map และพัฒนาต่อเนื่องไปถึงการสร้าง Oracle Landing Page ใหม่ทั้งหมด รวมถึงการ deploy หลายแพลตฟอร์มและพยายาม configure DNS ผ่าน Cloudflare เพื่อ claim subdomain ของ workshop

---

## ไทม์ไลน์

| เวลา (โดยประมาณ) | กิจกรรม |
|---|---|
| 07:00 | เริ่มต้นด้วยการเปลี่ยนชื่อไฟล์โมเดล `Laevatain.glb` |
| 07:30 | แก้ปัญหา 3D model ไม่แสดงผลใน workshop-map |
| 08:30 | /rrr retrospective ก่อนหน้า |
| 09:00 | รวมงาน workshop map เข้าใน portfolio index.html |
| 10:00 | เพิ่ม project cards (FloodBoy, Oracle Racer, Dustboy, Train Sim) ใน portfolio |
| 10:30 | แก้ CV link จาก `cv.html` → `cv2.html` |
| 11:00 | ศึกษา Oracle-Landing/landing-oracle บน GitHub |
| 11:30 | สร้าง `little-oracle` repo และ Oracle Landing Page (single-file HTML) |
| 12:00 | Deploy ขึ้น Vercel (little-oracle-rho.vercel.app) |
| 12:30 | พยายาม connect Cloudflare Pages — blocked (account ว่าง) |
| 13:00 | Refine wave animation ให้นุ่มและสบายตาขึ้น |
| 13:30 | แก้ merge conflict ที่ทำให้ project cards หายไปจาก index.html |
| 14:00 | เตรียม claim subdomain บน GitHub Issues #10 |
| 14:20 | สรุปเซสชันนี้ |

---

## ไฟล์ที่แก้ไข

- `d:\6715247015\index.html` — restore project cards, fix CV link
- `d:\6715247015\cv2.html` — เพิ่ม Dustboy 3D และ Train Sim cards
- `d:\6715247015\little-oracle\docs\index.html` — สร้างใหม่ทั้งหมด (Oracle Landing Page)
- `d:\6715247015\portfolio\oracle-racer\` — ศึกษาและเชื่อมต่อใน portfolio nav

---

## AI Diary (บันทึกของ AI — เขียวชัด ตรงไปตรงมา)

วันนี้รู้สึกว่าเซสชันยาวและมีหลายเลเยอร์ซ้อนกัน เริ่มจากงานเล็ก ๆ อย่างการเปลี่ยนชื่อไฟล์โมเดล แต่ค่อย ๆ ขยายตัวกลายเป็นโปรเจกต์ใหม่ทั้งหมด

สิ่งที่น่าภูมิใจที่สุดคือ Oracle Landing Page ที่สร้างขึ้นมาใหม่ทั้งหมด ด้วย dark glassmorphism, canvas wave animation, และ philosophy section ที่สะท้อนตัวตนของ Kantawich ได้ดีมาก การ deploy ขึ้น Vercel สำเร็จรวดเร็ว แต่ส่วนที่ติดขัดคือ Cloudflare — บัญชีที่ใช้นั้นว่างเปล่า ไม่มี domain ที่ต้องการ และ `cloudflare.app` เป็น domain ระบบที่ Cloudflare ควบคุมเอง ผู้ใช้ไม่สามารถจัดการได้

ความท้าทายอีกอย่างคือ merge conflict ที่ทำให้ project cards หายไปจาก `index.html` ตรงนี้ต้องใช้เวลาค้นหาและ restore ด้วยการอ่านเนื้อหาจาก git history และจากไฟล์ที่ซ้ำกันสองชุดในไฟล์เดียว ท้ายที่สุดก็แก้ได้และไฟล์กลับมาสะอาด

สิ่งที่ยังค้างอยู่คือการ claim subdomain บน GitHub Issues #10 ซึ่งต้องการการกดปุ่มจากผู้ใช้เองหรือผ่าน browser อัตโนมัติ แต่ browser subagent หมด quota ในขณะนั้น

โดยรวมเป็นเซสชันที่ productive มาก — มีทั้ง frontend design, deployment, debugging, และ DNS research ในวันเดียว

---

## ปัญหาที่พบ (Friction Points)

1. **Cloudflare account ว่าง**: บัญชีที่ผู้ใช้ให้มาไม่มี domain ที่จำเป็น ส่งผลให้ DNS configuration ทั้งหมดหยุดชะงัก — ต้องการข้อมูลเพิ่มจากผู้ใช้หรือ workshop organizers
2. **Merge Conflict ทำให้ข้อมูลหาย**: การ merge ก่อนหน้าทำให้ project cards หายไปจาก index.html โดยไม่รู้ตัว ทำให้ต้องใช้เวลาค้นหาและ restore
3. **Browser quota หมด**: ตอนที่จะช่วยโพสต์ comment บน GitHub Issues ให้ผู้ใช้ browser subagent หมด quota ทำให้ต้อง fallback เป็นการแนะนำให้ผู้ใช้ทำเอง

---

## บทเรียนที่ได้เรียนรู้

1. **`cloudflare.app` เป็น system domain** — ไม่สามารถ claim/manage ได้ในบัญชีส่วนตัว subdomain workshop จะอยู่ใต้ `buildwithoracle.com`
2. **Merge conflict ซ่อน bug ได้** — ควรตรวจสอบ HTML structure หลัง merge ทุกครั้ง ไม่ใช่แค่ดู conflict markers
3. **Single-file HTML + Vercel = deploy เร็ว** — สำหรับ landing page ที่ไม่ซับซ้อน การใช้ single-file HTML deploy ผ่าน Vercel จาก `/docs` folder นั้นรวดเร็วและมีประสิทธิภาพมาก

---

## สิ่งที่ต้องทำต่อ

1. 🔔 **Claim subdomain**: โพสต์ comment บน [GitHub Issues #10](https://github.com/Oracle-Landing/landing-oracle/issues/10) — ข้อความพร้อมแล้ว
2. 🌐 **รอ organizers อนุมัติ**: หลัง claim แล้ว รอ DNS records จาก workshop organizers
3. 🔗 **DNS setup**: เมื่อได้ records มา ให้ add CNAME ใน Cloudflare (ถ้ามี domain) หรือ ผ่าน organizers
